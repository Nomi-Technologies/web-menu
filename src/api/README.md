# a_collection_of_nomis_ap_is

ACollectionOfNomisApIs - JavaScript client for a_collection_of_nomis_ap_is
No description provided (generated by Swagger Codegen https://github.com/swagger-api/swagger-codegen)
This SDK is automatically generated by the [Swagger Codegen](https://github.com/swagger-api/swagger-codegen) project:

- API version: 1.0.0
- Package version: 1.0.0
- Build package: io.swagger.codegen.languages.JavascriptClientCodegen

## Installation

### For [Node.js](https://nodejs.org/)

#### npm

To publish the library as a [npm](https://www.npmjs.com/),
please follow the procedure in ["Publishing npm packages"](https://docs.npmjs.com/getting-started/publishing-npm-packages).

Then install it via:

```shell
npm install a_collection_of_nomis_ap_is --save
```

##### Local development

To use the library locally without publishing to a remote npm registry, first install the dependencies by changing 
into the directory containing `package.json` (and this README). Let's call this `JAVASCRIPT_CLIENT_DIR`. Then run:

```shell
npm install
```

Next, [link](https://docs.npmjs.com/cli/link) it globally in npm with the following, also from `JAVASCRIPT_CLIENT_DIR`:

```shell
npm link
```

Finally, switch to the directory you want to use your a_collection_of_nomis_ap_is from, and run:

```shell
npm link /path/to/<JAVASCRIPT_CLIENT_DIR>
```

You should now be able to `require('a_collection_of_nomis_ap_is')` in javascript files from the directory you ran the last 
command above from.

#### git
#
If the library is hosted at a git repository, e.g.
https://github.com/YOUR_USERNAME/a_collection_of_nomis_ap_is
then install it via:

```shell
    npm install YOUR_USERNAME/a_collection_of_nomis_ap_is --save
```

### For browser

The library also works in the browser environment via npm and [browserify](http://browserify.org/). After following
the above steps with Node.js and installing browserify with `npm install -g browserify`,
perform the following (assuming *main.js* is your entry file, that's to say your javascript file where you actually 
use this library):

```shell
browserify main.js > bundle.js
```

Then include *bundle.js* in the HTML pages.

### Webpack Configuration

Using Webpack you may encounter the following error: "Module not found: Error:
Cannot resolve module", most certainly you should disable AMD loader. Add/merge
the following section to your webpack config:

```javascript
module: {
  rules: [
    {
      parser: {
        amd: false
      }
    }
  ]
}
```

## Getting Started

Please follow the [installation](#installation) instruction and execute the following JS code:

```javascript
var ACollectionOfNomisApIs = require('a_collection_of_nomis_ap_is');

var defaultClient = ACollectionOfNomisApIs.ApiClient.instance;

// Configure API key authorization: Bearer
var Bearer = defaultClient.authentications['Bearer'];
Bearer.apiKey = "YOUR API KEY"
// Uncomment the following line to set a prefix for the API key, e.g. "Token" (defaults to null)
//Bearer.apiKeyPrefix['Authorization'] = "Token"

var api = new ACollectionOfNomisApIs.DefaultApi()

var path = "path_example"; // {String} path of assets to be fetched


var callback = function(error, data, response) {
  if (error) {
    console.error(error);
  } else {
    console.log('API called successfully.');
  }
};
api.apiAssetsPathGet(path, callback);

```

## Documentation for API Endpoints

All URIs are relative to *https://nomi-menu-service.herokuapp.com*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*ACollectionOfNomisApIs.DefaultApi* | [**apiAssetsPathGet**](docs/DefaultApi.md#apiAssetsPathGet) | **GET** /api/assets/{path} | 
*ACollectionOfNomisApIs.DishApi* | [**apiDishesDishIdDelete**](docs/DishApi.md#apiDishesDishIdDelete) | **DELETE** /api/dishes/{dishId} | 
*ACollectionOfNomisApIs.DishApi* | [**apiDishesDishIdGet**](docs/DishApi.md#apiDishesDishIdGet) | **GET** /api/dishes/{dishId} | 
*ACollectionOfNomisApIs.DishApi* | [**apiDishesDishIdPut**](docs/DishApi.md#apiDishesDishIdPut) | **PUT** /api/dishes/{dishId} | 
*ACollectionOfNomisApIs.DishApi* | [**apiDishesGet**](docs/DishApi.md#apiDishesGet) | **GET** /api/dishes | Fetches the menu information for the user's restaurant.
*ACollectionOfNomisApIs.DishApi* | [**apiDishesPost**](docs/DishApi.md#apiDishesPost) | **POST** /api/dishes | Creates a dish.
*ACollectionOfNomisApIs.DishApi* | [**webApiDishesRestaurantIdGet**](docs/DishApi.md#webApiDishesRestaurantIdGet) | **GET** /webApi/dishes/{restaurantId} | Fetches the published menu info of a restaurant
*ACollectionOfNomisApIs.RestaurantApi* | [**apiRestaurantsRegisterPost**](docs/RestaurantApi.md#apiRestaurantsRegisterPost) | **POST** /api/restaurants/register | Creates a restaurant.
*ACollectionOfNomisApIs.RestaurantApi* | [**webApiRestaurantsGet**](docs/RestaurantApi.md#webApiRestaurantsGet) | **GET** /webApi/restaurants | 
*ACollectionOfNomisApIs.UserApi* | [**apiUserLoginPost**](docs/UserApi.md#apiUserLoginPost) | **POST** /api/user/login | Log in.
*ACollectionOfNomisApIs.UserApi* | [**apiUserRegisterPost**](docs/UserApi.md#apiUserRegisterPost) | **POST** /api/user/register | Registers a new user.


## Documentation for Models

 - [ACollectionOfNomisApIs.ApiResponse](docs/ApiResponse.md)
 - [ACollectionOfNomisApIs.Category](docs/Category.md)
 - [ACollectionOfNomisApIs.Dish](docs/Dish.md)
 - [ACollectionOfNomisApIs.Restaurant](docs/Restaurant.md)
 - [ACollectionOfNomisApIs.Tag](docs/Tag.md)
 - [ACollectionOfNomisApIs.TagDishTags](docs/TagDishTags.md)
 - [ACollectionOfNomisApIs.User](docs/User.md)


## Documentation for Authorization


### Bearer

- **Type**: API key
- **API key parameter name**: Authorization
- **Location**: HTTP header

